{
  "name": "ai-english-speaking-trainer",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev:backend": "npm run start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
